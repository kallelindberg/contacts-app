import { VibrationClickDirective } from './vibration-click.directive';

describe('VibrationClickDirective', () => {
  it('should create an instance', () => {
    const directive = new VibrationClickDirective();
    expect(directive).toBeTruthy();
  });
});
